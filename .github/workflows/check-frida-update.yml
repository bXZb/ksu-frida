# Disabled: the module no longer bundles a Frida gadget, so there is
# nothing to bump from gorkemgun/knox-frida-patcher.
name: Check Frida Updates

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - name: Bundled gadget removed
        run: |
          echo "KsuFrida no longer ships a gadget. Users install their own .so via WebUI."
          echo "This workflow is intentionally a no-op."
